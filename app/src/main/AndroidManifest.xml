<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="gamint.com.unityschoolsattendance">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Unboarding" />
        <activity android:name=".Splash" />
        <activity android:name=".FormLogin" />
        <activity android:name=".Settings" />
        <activity android:name=".DialogAddReview" />
        <activity android:name=".Licenses" />
        <activity android:name=".Login" />
        <activity android:name=".CameraCapture" />
        <activity android:name=".BioMiniMain" />
    </application>

</manifest>